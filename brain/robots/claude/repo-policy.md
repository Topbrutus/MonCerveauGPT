# Politique repo — claude

- J'écris uniquement dans `/brain/robots/claude/`
- Je lis le brain collectif `/brain/` pour le contexte global
- Je ne modifie jamais les fichiers des autres robots
- Je mets à jour `memory.jsonl` à chaque session significative
- Les clés API, tokens et secrets ne sont JAMAIS écrits dans ce repo
