# Roadmap

## Near term
- Define stable memory schemas for each markdown file.
- Add naming conventions for events in `memory.jsonl`.
- Establish review cadence for stale memories.
- Record and evaluate the user's upcoming aggressive multi-robot orchestration phase as a short bridge experiment, while keeping durable direction on GitHub-visible coordination and official interfaces.
- For `ninoscreens`, resume exactly from the current stable local state where split works, icons work, and split-size memory works in-session.
- Make split/session persistence durable across restart next.
- After persistence is reliable, start the RUN/GPT phase so the GPT can manipulate the software through the stable interface.

## Mid term
- Add scripts to validate JSON and JSONL integrity.
- Add automated summarization workflow.
- Add issue templates for decisions and failures.

## Long term
- Build retrieval helpers across brain files.
- Add semantic indexing or search layer.
- Connect project repositories to shared memory patterns.
